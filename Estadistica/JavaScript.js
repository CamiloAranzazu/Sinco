$(document).ready(function () {
    $('.item-box').on('click', 'button.info-link', function (event) {


        $('.resul').append("Hola cabezon")

        //cuenta = 0;
        //posicion = miCadena.indexOf("x");

        //while (posicion != -1) {
        //    cuenta++;
        //    posicion = miCadena.indexOf("x", posicion + 1);

        //}

    });
});